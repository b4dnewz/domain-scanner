const assert = require('assert');
const domainScanner = require('../index.js');

describe('domainScanner', () => {
  it('has a test', () => {
    assert(false, 'domainScanner should have a test');
  });
});
